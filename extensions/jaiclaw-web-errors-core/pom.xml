<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.jaiclaw</groupId>
        <artifactId>jaiclaw-extensions</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>jaiclaw-web-errors-core</artifactId>
    <name>JaiClaw :: Web Errors :: Core</name>
    <description>Shared types + pure mapping logic for the JaiClaw default web exception handlers. Zero Spring web-stack dependency — usable from either the WebMVC or WebFlux sibling module.</description>

    <dependencies>
        <!-- Only Spring dep is @ConfigurationProperties + @Bean annotations
             from spring-context, and even that is optional so this module
             compiles + tests without pulling any of the Spring web stacks. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test: Spock + Groovy — pure unit tests for the mapper. No
             web-stack test scope; that lives in the -mvc and -webflux modules. -->
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.spockframework</groupId>
            <artifactId>spock-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.gmavenplus</groupId>
                <artifactId>gmavenplus-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
