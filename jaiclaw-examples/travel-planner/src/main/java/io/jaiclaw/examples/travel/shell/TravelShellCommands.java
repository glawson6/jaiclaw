package io.jaiclaw.examples.travel.shell;

/**
 * TODO(spring-shell-4-migration): Quarantined during the Boot 4 upgrade.
 * Original Shell 3 {@code @ShellComponent}/{@code @ShellMethod} annotations
 * were auto-rewritten to Shell 4 {@code @Command}/{@code @Option} but the
 * mechanical rewrite corrupted string literals containing parentheses
 * ({@code "%s"} etc.) — needs manual re-implementation.
 * See docs/spring-boot-4-upgrade/06-spring-shell-4-migration.md.
 */
public class TravelShellCommands {
    // Body removed during Spring Boot 4 upgrade — see class javadoc.
}
