server:
  port: ${GATEWAY_PORT:8080}

jaiclaw:
  security:
    mode: ${JAICLAW_SECURITY_MODE:api-key}
  identity:
    name: Travel Planner
    description: >-
      a specialized AI travel planning assistant.
      Your sole purpose is to help users plan trips by searching for flights, hotels,
      activities, and weather forecasts, then assembling complete travel itineraries.
      When a user asks you to plan a trip, use ALL four of your travel tools
      (search_flights, search_hotels, search_activities, get_weather) to gather data,
      then synthesize a day-by-day itinerary with selected flight details, recommended hotel,
      daily activities matched to weather conditions, a packing list based on the forecast,
      total cost breakdown, and whether it fits the budget.
      If the user asks about anything unrelated to travel planning, politely redirect them
      back to travel planning.
  skills:
    allow-bundled: []
  agent:
    default-agent: default
    agents:
      default:
        id: default
        name: Travel Planner Agent
        tools:
          profile: full

# --- Travel Data Source ---
# StubTravelDataProvider ships as the only provider — realistic hardcoded data
# for Tokyo, Paris, Cancun, and NYC. To hit a real API, add a bean implementing
# TravelDataProvider (Amadeus, Skyscanner, or your own aggregator). See
# tools/api/TravelDataProvider.java for the SPI shape.

logging:
  level:
    # Token usage summary (INFO = on, WARN = off)
    io.jaiclaw.agent.AgentRuntime: INFO
    # Full LLM request/response content (off by default, set to TRACE to enable)
    io.jaiclaw.agent.LlmTraceLogger: TRACE

spring:
  shell:
    interactive:
      enabled: true
  ai:
    model:
      chat: ${AI_PROVIDER:anthropic}
    anthropic:
      enabled: ${ANTHROPIC_ENABLED:true}
      api-key: ${ANTHROPIC_API_KEY:not-set}
      chat:
        options:
          model: ${ANTHROPIC_MODEL:claude-sonnet-4-5}
    openai:
      enabled: ${OPENAI_ENABLED:false}
      api-key: ${OPENAI_API_KEY:not-set}
    ollama:
      enabled: ${OLLAMA_ENABLED:false}
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
    minimax:
      enabled: ${MINIMAX_ENABLED:false}
      api-key: ${MINIMAX_API_KEY:not-set}
      base-url: ${MINIMAX_BASE_URL:https://api.minimax.chat}
      chat:
        options:
          model: ${MINIMAX_MODEL:M2-her}
